// API Configuration
// En desarrollo: usar IP local (el dominio .test no resuelve desde el celular)
// En producción: cambiar a la URL del servidor
const API_CONFIG = {
  BASE_URL: "http://192.168.40.145:8000/api",
  TIMEOUT: 15000, // 15 segundos
};

export default API_CONFIG;
